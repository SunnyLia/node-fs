<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>在线聊天</title>
    <link rel="stylesheet" href="/layui/css/layui.css">
    <link rel="stylesheet" href="/common/common.css">

</head>

<body class="layui-layout-body">
    <div id="chatRoom">

        <div id="chatTop">
            <span>闲聊群 (12)</span>
            <i class="layui-icon layui-icon-close" onclick="roomClose()"></i>
        </div>
        <div id="chatList" style="margin: 50px 0;">
            <div class="chatItem" style="padding: 10px;">
                <div class="author">张三</div>
                <div class="time">
                    <span style="font-weight:bold">张三 </span>
                    2020/03/17 14:59
                </div>
                <div class="massage">
                    我是一个粉刷匠，粉刷本领强，
                    我是一个粉刷匠，粉刷本领强，
                    我是一个粉刷匠，粉刷本领强，
                </div>
            </div>
        </div>
        <div id="chatBotton">
            <input type="text" name="chatWord" class="layui-input">
            <button type="button" class="layui-btn">发送</button>
        </div>
    </div>
    <script src="/common/jquery-1.11.3.min.js"></script>
    <script src="/layui/layui.js"></script>
    <script type="text/javascript" src="/common/socket.io.min.js"></script>
    <script>
        var socket = io();
        socket.on('connect', function () {
            socket.emit('join', { id: getQuery("roomId") });
            
        });
        socket.on('chatLists', function (data) {
                console.log(data);
            });
        function roomClose() {
            var index = parent.layer.getFrameIndex(window.name);
            parent.layer.close(index);
        }
        function getQuery(name) {
            var reg = new RegExp('(^|&)' + name + '=([^&]*)(&|$)', 'i');
            var r = window.location.search.substr(1).match(reg);
            if (r != null) {
                return unescape(r[2]);
            }
            return null;
        }

    </script>
</body>

</html>